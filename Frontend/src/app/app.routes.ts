import { Routes } from '@angular/router';
import { DesktopComponent } from './components/os/desktop/desktop.component';

export const routes: Routes = [
    { path: '', component: DesktopComponent }
];
